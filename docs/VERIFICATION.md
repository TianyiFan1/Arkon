# 本地验证记录

验证日期：2026-09-10。环境：Windows、Node.js 24.12、真实 PostgreSQL 18、Chrome（Playwright 控制）。

| 检查 | 实际结果 |
| --- | --- |
| Vitest 单元、Schema、PostgreSQL 集成与 Route Handler | 7 个文件，45 个用例通过 |
| Playwright 真实浏览器与 HTTP | 3 个用例通过，最终运行约 7.9 秒 |
| Next.js 生产构建 | 通过 |
| TypeScript | 通过 |
| ESLint | 通过 |
| V8 覆盖率（src/lib 与 API routes，不含前端覆盖率） | statements 83.33%，branches 72.15%，functions 96.55%，lines 86.58% |

测试覆盖保存失败不跳步、刷新恢复、支付后会员解锁、旧版本冲突、评估失效、并发计算、事务回滚、套餐金额、支付重放及算法边界。

测试使用每轮新建的随机数据库，结束后自动清理，未对原 SQLite 数据做删除或转换。最终浏览器运行确认 Windows 服务自动退出，测试进程以 0 退出。

这些是本地证据；GitHub Actions 和公网部署尚无已核实的通过链接。后续代码或环境变更后，应重新执行 README 中的命令。


## 全页面重设计复验

2026-09-10 晚间：采用 `frontend-design`，设计依据见 `DESIGN.md`。

- 最终生产构建、TypeScript、ESLint 均通过。
- 最终 Playwright 4 个用例全部通过，16.3 秒，退出码 0。涵盖原有保存与恢复、支付解锁、冲突和非法请求；新增移动端方向键选择、中英切换、英制换算往返与计算失败重试。
- 本轮浏览器测试连接本机开发 PostgreSQL，创建独立的新测评，不修改固定会员示例；未重新运行后端 45 个用例及覆盖率，上表对应此前后端验证。
- 实际检查四步填写、计算中、错误、免费结果、会员结果、演示工具及 404 的截图；检查 1440px 桌面、390px 手机，以及 320px 英文会员页。小屏未出现横向溢出。
- 完整测评截图流程中无浏览器运行时错误和 hydration 错误。弧形刻度使用固定三位小数避免跨运行时浮点差异。
- 本地截图保存在被忽略的 `.local/design-review/`。公开部署状态不变。
